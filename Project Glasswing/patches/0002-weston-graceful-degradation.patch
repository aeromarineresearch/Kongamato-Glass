diff --git a/src/pyefis/instruments/weston/__init__.py b/src/pyefis/instruments/weston/__init__.py
index 26de4d1..a2fb4e9 100644
--- a/src/pyefis/instruments/weston/__init__.py
+++ b/src/pyefis/instruments/weston/__init__.py
@@ -5,6 +5,10 @@ import subprocess
 import time
 import re
 import os
+import shutil
+import logging
+
+log = logging.getLogger(__name__)
 
 try:
     from Xlib import X, display
@@ -20,6 +24,20 @@ class Weston(QWidget):
         layout.setContentsMargins(0, 0, 0, 0)
         self.setLayout(layout)
 
+        # Weston + X11 tooling (weston compositor, xwininfo, python-xlib) is
+        # only available on Linux. On other platforms degrade gracefully with
+        # a placeholder instead of crashing the whole application.
+        if (
+            display is None
+            or shutil.which('weston') is None
+            or shutil.which('xwininfo') is None
+        ):
+            log.warning("Weston/X11 tooling not available on this platform; showing placeholder")
+            label = QLabel("Weston unavailable on this platform", self)
+            label.setAlignment(Qt.AlignmentFlag.AlignCenter)
+            layout.addWidget(label)
+            return
+
         self.weston = QProcess(self)
         westenv = QProcessEnvironment.systemEnvironment()
             
